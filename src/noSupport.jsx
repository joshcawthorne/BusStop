import React, { Component } from "react";

export class noSupport extends Component {
  render() {
    return <div>Error: Your Browser Doesn't support GeoLocation.</div>;
  }
}

export default noSupport;
